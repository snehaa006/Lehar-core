rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend-only access - all operations go through Flask backend
    // The backend uses admin SDK which bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
